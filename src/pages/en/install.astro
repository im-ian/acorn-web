---
import Base from "~/layouts/Base.astro";
import InstallPage from "~/components/InstallPage.astro";

const lang = "en" as const;
---

<Base lang={lang} title="Install — Acorn" description="Install Acorn via the install script, manual DMG, or from source.">
  <InstallPage lang={lang} />
</Base>
