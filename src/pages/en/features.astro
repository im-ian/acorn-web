---
import Base from "~/layouts/Base.astro";
import FeaturesPage from "~/components/FeaturesPage.astro";

const lang = "en" as const;
---

<Base lang={lang} title="Features — Acorn" description="Pane, Kanban, and Canvas workspaces, native AI chat, isolated worktrees, work summaries, control sessions, and more.">
  <FeaturesPage lang={lang} />
</Base>
