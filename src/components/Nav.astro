---
import { useTranslations, type Lang } from "~/i18n/ui";
import { asset, localeHref, links } from "~/lib/site";
import LangToggle from "~/components/LangToggle.astro";

interface Props {
  lang: Lang;
  path: string;
}
const { lang, path } = Astro.props;
const t = useTranslations(lang);
---

<header class="nav">
  <div class="container nav__inner">
    <a class="nav__brand" href={localeHref(lang, "")}>
      <img src={asset("acorn-icon-256.png")} width="24" height="24" alt="" />
      <span>Acorn</span>
    </a>

    <nav class="nav__links" aria-label="Main">
      <a href={localeHref(lang, "features")}>{t("nav.features")}</a>
      <a href={localeHref(lang, "install")}>{t("nav.install")}</a>
      <a href={links.repo} target="_blank" rel="noopener">{t("nav.docs")}</a>
    </nav>

    <div class="nav__right">
      <LangToggle lang={lang} path={path} />
      <a class="nav__icon" href={links.repo} target="_blank" rel="noopener" aria-label="GitHub">
        <svg width="18" height="18" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
          <path
            d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"
          ></path>
        </svg>
      </a>
      <a class="pill pill--primary nav__dl" href={links.releasesLatest} target="_blank" rel="noopener">
        {t("nav.download")}
      </a>
    </div>
  </div>
</header>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in oklab, var(--bg) 80%, transparent);
    backdrop-filter: saturate(180%) blur(10px);
    border-bottom: 1px solid var(--border);
  }
  .nav__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 56px;
    gap: 1rem;
  }
  .nav__brand {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 600;
    font-size: 0.95rem;
  }
  .nav__links {
    display: flex;
    gap: 1.4rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--muted);
  }
  .nav__links a:hover {
    color: var(--ink);
  }
  .nav__right {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }
  .nav__icon {
    display: inline-flex;
    align-items: center;
    color: var(--muted);
    transition: color 0.15s ease;
  }
  .nav__icon:hover {
    color: var(--ink);
  }
  .nav__dl {
    padding: 0.5rem 0.95rem;
    font-size: 0.8125rem;
  }
  @media (max-width: 720px) {
    .nav__links {
      display: none;
    }
  }
  @media (max-width: 480px) {
    .nav__dl {
      display: none;
    }
  }
</style>
